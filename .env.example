# Database
# Required for production - set to your production database URL (e.g., Neon, Railway, etc.)
# Example for Neon: postgresql://username:password@ep-example-123.us-east-1.aws.neon.tech/writarcade?sslmode=require
DATABASE_URL="postgresql://username:password@localhost:5432/writarcade"

# Auth session secret (Server-side only - DO NOT expose to client)
# HMAC key for signing wallet_session cookies. REQUIRED in production (>= 16 chars).
# Generate one with: openssl rand -hex 32
AUTH_SECRET="generate-a-random-secret-min-16-chars"

# Site URL (Client-side)
# Used for OpenGraph and Twitter Card meta tags
# Set to your production domain (e.g., https://writarcade.vercel.app)
NEXT_PUBLIC_SITE_URL="https://writarcade.vercel.app"
NEXT_PUBLIC_ANALYTICS_ENDPOINT="/api/analytics"

# Flynn iMessage line (Client-side)
# The managed Photon line's public address (phone "+1..." or iMessage email).
# When set, "text it to Flynn" links appear on the landing + generate pages.
NEXT_PUBLIC_FLYNN_IMESSAGE=""

# Neynar API (Server-side only - DO NOT expose to client)
# Used in /api/farcaster/profile for Farcaster user profile lookup
NEYNAR_API_KEY="your-neynar-api-key"

# WalletConnect Project ID (Client-side)
# Used in Web3Provider for RainbowKit wallet connections
# Get this from https://cloud.walletconnect.com
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID="your-walletconnect-project-id"

# Smart Contract Addresses (safe to expose - read-only)
# Used in lib/contracts.ts for contract interactions

# Base Sepolia (Testnet)
NEXT_PUBLIC_WRITER_COIN_PAYMENT_SEPOLIA="0x..."
NEXT_PUBLIC_GAME_NFT_SEPOLIA="0x..."

# Base Mainnet (Production)
NEXT_PUBLIC_WRITER_COIN_PAYMENT_MAINNET="0x56Ee5A3f122da00B635DdbB319708e24450aEB89"
NEXT_PUBLIC_GAME_NFT_MAINNET="0x32D0356f533cC429F94Db73f383bBb21a459E16b"
NEXT_PUBLIC_WRITER_COIN_PAYMENT_ADDRESS="0x56Ee5A3f122da00B635DdbB319708e24450aEB89"
NEXT_PUBLIC_GAME_NFT_ADDRESS="0x32D0356f533cC429F94Db73f383bBb21a459E16b"

# Network Chain IDs (safe to expose)
NEXT_PUBLIC_BASE_SEPOLIA_CHAIN_ID="84532"
NEXT_PUBLIC_BASE_MAINNET_CHAIN_ID="8453"

# ============================================
# Mezo Network (Bitcoin-backed MUSD payments)
# ============================================
# Mezo Matsnet (testnet) chainId: 31611
# Public RPC: https://rpc.test.mezo.org (default if override not set)
# Override RPC if you have a dedicated hackathon endpoint:
NEXT_PUBLIC_MEZO_TESTNET_RPC=""

# Deployed MezoPaymentSplitter contract addresses (safe to expose - read-only)
# Override the bundled defaults if you redeploy.
NEXT_PUBLIC_MEZO_PAYMENT_SPLITTER_TESTNET="0x32D0356f533cC429F94Db73f383bBb21a459E16b"
NEXT_PUBLIC_MEZO_PAYMENT_SPLITTER_MAINNET=""

# Server-side only - DO NOT commit. Used by scripts/deploy-mezo.sh
# A funded Mezo testnet wallet (gas in BTC, plus MUSD for end-to-end tests)
MEZO_DEPLOYER_PRIVATE_KEY=""
# Address that receives the platform's share of MUSD payments
PLATFORM_TREASURY_ADDRESS=""
# Mezo testnet MUSD token (default in lib/writerCoins.ts MUSD_CONFIG.testnet.address)
MEZO_TESTNET_MUSD_ADDRESS="0x118917a40FAF1CD7a13dB0Ef56C86De7973Ac503"

# Venice AI API Key (Server-side only - DO NOT expose to client)
# Used in /api/generate-image for secure image generation
VENICE_API_KEY="your-venice-api-key"

# Netmind AI API Key (Server-side only - Fallback image provider)
# Used when Venice API fails or runs out of credits
# Get this from https://docs.netmind.ai/
NETMIND_API_KEY="your-netmind-api-key"

# Modal Image Generation (Server-side only - Self-hosted fallback)
# Your deployed Modal endpoint for Stable Diffusion image generation
# Deploy with: cd scripts/modal && modal deploy modal_image_gen.py
# IMPORTANT: Keep this URL private! Anyone with it can use your GPU credits.
# Get your URL from Modal dashboard after deployment
MODAL_IMAGE_GEN_URL="your-modal-endpoint-url-here"

# Google AI API Key (Server-side only - DO NOT expose to client)
# Used for Gemini 3 integration (optional - users can BYOK)
GOOGLE_API_KEY="your-google-ai-api-key"

# Story Protocol Integration (Client-Side)
# Users sign IP registration with their own wallets (no server keys needed)
# Faucet for testnet tokens: https://faucet.story.foundation/

# Story Protocol Network ("testnet" or "mainnet")
# Testnet Chain ID: 1315 (Aeneid)
# Mainnet Chain ID: 1513
NEXT_PUBLIC_STORY_NETWORK="testnet"

# Server-side platform key for Story Protocol (IP registration gas)
# DO NOT expose to client.
STORY_PLATFORM_PRIVATE_KEY="your-platform-private-key"

# SPG (Story Protocol Gateway) Contract for fast minting
# Default testnet: 0xc32A8a0FF3beDDDa58393d022aF433e78739FAbc
NEXT_PUBLIC_STORY_SPG_CONTRACT="0xc32A8a0FF3beDDDa58393d022aF433e78739FAbc"

# Enable/disable Story Protocol UI (for development)
NEXT_PUBLIC_STORY_ENABLED="true"

# IPFS Storage (for game/asset metadata)
# Pinata is primary; Grove immutable uploads are the production fallback.
PINATA_JWT="your-pinata-jwt-token"
IPFS_GATEWAY="https://gateway.pinata.cloud"
GROVE_CHAIN_ID="8453"

# OpenAI API Key (Server-side only - DO NOT expose to client)
# Used for AI text generation and TTS (Text-to-Speech) narration
# Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key"

# ElevenLabs API Key (Server-side only - OPTIONAL)
# Premium TTS provider for higher quality voice narration
# Get this from https://elevenlabs.io
ELEVENLABS_API_KEY="your-elevenlabs-api-key"
ELEVENLABS_DEFAULT_VOICE_ID="21m00Tcm4TlvDq8ikWAM"

# ============================================
# Video hero artifact pipeline (server-side only)
# Runware is preferred when configured; Luma, fal, and Replicate are fallbacks.
RUNWARE_API_KEY="your-runware-api-key"
RUNWARE_VIDEO_MODEL="klingai:kling-video@3-standard"
RUNWARE_VIDEO_DURATION="5"
# Optional explicit provider order override: runware | luma | fal | replicate
VIDEO_PROVIDER="runware"
LUMA_API_KEY="your-luma-api-key"
LUMA_VIDEO_MODEL="ray-2"
# Video adapter accepts either name; FAL_API_KEY is shared with image generation.
FAL_KEY="your-fal-api-key"
FAL_VIDEO_MODEL="fal-ai/stable-video-diffusion"
REPLICATE_API_TOKEN="your-replicate-api-token"
REPLICATE_VIDEO_MODEL="luma/ray-2"
FEATURE_VIDEO_PIPELINE="true"
# Must also be true for the browser to show the optional animation CTA.
NEXT_PUBLIC_FEATURE_VIDEO_PIPELINE="true"
# Lock a type-free "real scene" first frame before I2V (storyboard-first).
# Set to "false" to animate the comic panel directly.
VIDEO_PRE_PRODUCTION_STILL="true"

# ============================================
# Inco — Confidential Compute for Secret Panels
# ============================================
# Encrypted secret panels and Wordle answers on Base mainnet.
# No WASM, no separate testnet, no backend proxy required.
# SDK: @inco/lightning-js (client) + @inco/lightning (Solidity — install via `bun add @inco/lightning`)
# Docs: https://docs.inco.org
#
# Feature flag (default ON — set to "false" to fall back to legacy CDR)
FEATURE_INCO="true"
# Network: "baseMainnet" (production) or "baseSepoliaTestnet" (dev/test)
NEXT_PUBLIC_INCO_NETWORK="baseMainnet"
# SecretPanelVault contract address (deploy contracts/src/SecretPanelVault.sol)
# Base mainnet deploy: 0x36a3931f1acb69033f98e6eb8c3aa7d59cc6e5e8
NEXT_PUBLIC_SECRET_PANEL_VAULT_ADDRESS="0x..."
# Base RPC URL (optional — Inco SDK uses viem's default if not set)
# NEXT_PUBLIC_BASE_RPC_URL="https://mainnet.base.org"

# ============================================
# Daily Challenge (Inco confidential game sessions + BasePaint crossover)
# ============================================
# Feature flag — set to "true" to enable the /daily page and modifier system
FEATURE_DAILY_CHALLENGE="false"
# Must be "true" in production for client components (browser cannot read FEATURE_* alone)
NEXT_PUBLIC_FEATURE_DAILY_CHALLENGE="false"
# DailyChallengeVault contract address (deploy contracts/src/DailyChallengeVault.sol)
# Base mainnet deploy: 0x0bb738ee11839baa44aa46984997f9417733dcce
NEXT_PUBLIC_DAILY_CHALLENGE_VAULT_ADDRESS="0x..."
# BasePaint Hackathon — mint referrer on outbound basepaint.xyz links
NEXT_PUBLIC_BASEPAINT_REFERRER_ADDRESS="0x..."
# Dual-source Daily — featured article (plot). Env is bootstrap/fallback only.
DAILY_CHALLENGE_FEATURED_ARTICLE_URL=""
DAILY_CHALLENGE_FEATURED_ARTICLE_TITLE=""
# Auto-pick from Paragraph allowlist on daily cron (default on). Set "false" to disable.
DAILY_CHALLENGE_AUTO_FEATURED="true"
# Comma-separated publication slugs (no @). Empty = WRITER_COINS.paragraphAuthor list.
DAILY_CHALLENGE_FEATURED_PUBLICATIONS=""
# Don't re-feature the same article URL within this many prior days
DAILY_CHALLENGE_FEATURED_LOOKBACK_DAYS="14"
# Server wallet with SESSION_MANAGER_ROLE (for deck shuffle + recordChoice)
# Must hold Base ETH for Inco list fees. Health: GET /api/daily-challenge/status
# Can reuse INCO_VAULT_MANAGER_PRIVATE_KEY or STORY_PLATFORM_PRIVATE_KEY
DAILY_CHALLENGE_MANAGER_PRIVATE_KEY="0x..."

# Vercel Cron auth for daily deck shuffle (GET /api/daily-challenge/setup)
# Generate: openssl rand -hex 32
CRON_SECRET=""

# ============================================
# Hypercerts (Impact Certificates via AT Protocol)
# ============================================
# Creates impact certificates for game creation.
# Set to "false" to disable (uses mock URIs in dev)
HYPERCERTS_ENABLED="true"

# AT Protocol credentials (from certified.app)
# Without these, mock URIs are generated for development
HYPERCERTS_HANDLE="your-handle.certified.app"
HYPERCERTS_APP_PASSWORD="your-app-password"
HYPERCERTS_PDS_URL="https://certified.app"

# Hugging Face Inference API (free tier available)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# ============================================
# Etherfuse Ramp API (Fiat Onramp)
# ============================================
# Get your API key from https://devnet.etherfuse.com (sandbox) or https://app.etherfuse.com (production)
ETHERFUSE_API_KEY="your-etherfuse-api-key"

# Etherfuse API base URL (sandbox vs production)
# Sandbox: https://api.sand.etherfuse.com
# Production: https://api.etherfuse.com
ETHERFUSE_API_URL="https://api.sand.etherfuse.com"

# Webhook secret for verifying incoming webhook signatures
ETHERFUSE_WEBHOOK_SECRET="your-webhook-secret"

# ============================================
# SuperRare NFT Minting
# ============================================
# SuperRare API key for minting and gallery queries
SUPERRARE_API_KEY="your-superrare-api-key"

# SuperRare API base URL
SUPERRARE_API_URL="https://api.superrare.com"

# SuperRare shared minting contract (defaults to SuperRareV2: 0xb932a70a57673d89f4acffbe830e8ed7f75fb9e0)
# Override if using a different contract (e.g., Series contract)
SUPERRARE_CONTRACT_ADDRESS="0xb932a70a57673d89f4acffbe830e8ed7f75fb9e0"

# ============================================
# iMessage Agent
# ============================================
# Shared secret the iMessage agent uses to call /api/imessage/generate
IMESSAGE_API_SECRET="change-me-to-a-long-random-string"
